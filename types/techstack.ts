export type TechEntry = {
  img: string;
  label: string;
  color: string;
  invert?: boolean;
  desc: string;
  use: string;
};

export type DevOpsTheme = "galaxy" | "nebula" | "solar" | "aurora" | "orchid" | "frost";
